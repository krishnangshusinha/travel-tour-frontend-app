import React from "react";
import ReactDOM from "react-dom";
import App from './App.js';

/* React-icons package is being used for icons in this website. npm i react-icons */

ReactDOM.render(
  <App/>,
  document.getElementById('root')
);